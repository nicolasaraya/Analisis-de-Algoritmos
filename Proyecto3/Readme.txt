Compilar utilizando:  g++ main.cpp
Ejecutar utilizando la siguiente estructura ./a.out n a b

Siendo n la cantidad de vértices que se quieren añadir al grafo, y a y b los límites del intervalo en el que se encontraran los valores de cada vértice. 

El resultado de la ejecucion sera el grafo generado junto con las soluciones obtenidas con TopDown y BottomUp
